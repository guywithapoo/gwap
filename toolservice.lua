

function module.new(tool) -- tool OR table that includes all the info needed
  -- "tool" module script inside tool that you search for and require if found, provides a new UI that u can use inside the tool UI as a custom thing
  
  
  


end
